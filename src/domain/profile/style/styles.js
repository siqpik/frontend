import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
    gridView: {
        borderWidth: 2,
        borderColor: 'red',
    },
    picStyle: {
        justifyContent: 'flex-end',
        width: 120,
        height: 140,
    },
    profilePic: {
        width: 200,
        height: 200,
        borderRadius: 100,
        borderWidth: 2,
        borderColor: 'black'
    }
});
